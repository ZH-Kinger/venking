# 协作记账 notes.md

> 每行:`[日期] [角色] 决策/发现`。角色:DEV / RESEARCHER / PLANNER / TESTER / AUDITOR / DOCWRITER

- [2026-07-12] [DEV] 项目定调:给博客(ZH-Kinger, VuePress, 205 篇 md)做 Python+LangChain 的 **Agentic RAG**,语料=博客文章。用户目标是**边学边做**,dev 讲理论+搭骨架,用户主写。计划见 plan 文件 starry-tumbling-globe.md。
- [2026-07-12] [DEV] 用户三项已定:①Agentic RAG(进阶) ②Python+LangChain ③边讲理论边写。Provider 倾向 Qwen/阿里云百炼(OpenAI 兼容),向量库 Chroma,Embedding 原计划本地 text2vec。
- [2026-07-12] [DEV] 新增语料挑战:`AI INFRA SRE.pdf`(17MB, **333 页**, 文字层可提取约 24 万字, **69 张图其中 62 张为大架构图**, 文字层有 BF16→`BF\x00\x00` 类脏数据)。→ M1 升级为异构数据入库(md + PDF文字清洗 + 图片多模态)。
- [2026-07-12] [DEV] 环境事实:本机 **Python 3.14.0**,仅装 pillow;探测用临时装了 pypdf。担忧:torch/sentence-transformers 可能无 3.14 wheel → 影响本地 text2vec 方案。
- [2026-07-12] [DEV] 自省:此前 PDF 侦察与多模态方案均单干,违反 /team 分派纪律。现补派 researcher 取证。
- [2026-07-12] [RESEARCHER] R1 多模态方案(报告 docs/collab/research/R1-多模态RAG方案.md):PDF 用裸 PyMuPDF(fitz) 一遍抽文字+图+页码;图片走 **B(Qwen-VL caption 成中文入文本库)+D(命中带出原图)**,零新依赖同栈;VL 模型 `qwen-vl-max`/`qwen-vl-plus`,OpenAI 兼容 image_url/base64,base_url=dashscope 标准兼容端点,69 张图 caption <1 元。原生多模态 embedding(路线C)效果更好但需 torch+独立 collection,排后。
- [2026-07-12] [RESEARCHER] R2 环境+embedding(报告 docs/collab/research/R2-环境与embedding.md):**实测 Python 3.14 能装 torch(torch 2.13.0 有 cp314 wheel)**,dev 先前担忧不成立→本地 text2vec 可行。但建议 M1 用百炼 **text-embedding-v4**(1024维默认,OpenAI 兼容,免费 100万token/90天,便宜)。**LangChain 坑**:OpenAIEmbeddings 默认 batch 1000>百炼上限10,须 `chunk_size=10, check_embedding_ctx_length=False`,或用 DashScopeEmbeddings。维度选定即锁定(改维度须重建库)。
- [2026-07-12] [DEV] 用户改用 **GLM-5.2**,走自定义 MaaS 兼容端点(ws-...maas.aliyuncs.com);embedding/VL 走百炼标准端点(dashscope.aliyuncs.com)。→ config 按用途分 base_url。M0 起步:venv(rag-server/.venv)+openai 2.45.0+python-dotenv 已装。
- [2026-07-12] [DEV] M0 smoke **通过**(GLM-5.2 流式返回,分离 reasoning/content)。开始标准化重构:src-layout 包 `src/blog_rag/` + pyproject + `pip install -e .`,config/llm 迁入。
- [2026-07-12] [DEV] 计划定稿 v2:扩到 M0–M8,新增 M6 对话UI(Gradio)+反馈闭环、M5 双模式路由、全参数进 .env。plan=starry-tumbling-globe.md。
- [2026-07-12] [DEV] 用户"所有模型换 GLM-5.2":语言/推理任务(含 **LLM 重排、看图若多模态**)全切 GLM-5.2;**embedding 必须保留专门模型**(text-embedding-v4,对话模型做不了向量化);VL 默认 GLM-5.2 待 M1 实测端点多模态,不支持则回退。已改 config。
- [2026-07-12] [DEV] 产出《项目总结-方向与技术选型》(docs/collab/),含方向/选择原因/难度/方案对比表。待决:GLM 端点是否多模态、同一 key 能否同打 MaaS+百炼——M1 实测。
- [2026-07-12] [DEV] 知识库存储规范定稿(docs/collab/知识库存储规范.md,已核实API):**LangChain Indexing API + SQLRecordManager 管增改删同步** + 薄 manifest.json 管模型/维度/分块版本闸(Indexing API 察觉不到换 embedding)。Chroma delete 只吃 id,按 metadata 删用 get(where)+delete。
- [2026-07-12] [DEV] **M0 完成**:标准化 src-layout 包 + pip install -e . + 导入验证通过。config **重构为 pydantic-settings**(30 旋钮全 env 驱动 + 类型校验,实测 TOP_K=abc 被拦)。旧扁平 config.py/llm.py/requirements.txt 已删。.env 已确认被 gitignore、未被跟踪。
- [2026-07-12] [DEV] 纪律新增:rag-server/README.md 由 **docwriter 持续维护**,每模块完成更新一次(纳入 DoD)。
- [2026-07-12] [DEV] 参数策略:config 里的值是**有依据的默认值(未在本语料实测)**,来自研究+用户笔记+最佳实践。先用默认,**M3 评测上线后用数据调优**(全在 .env,调参零代码改动)。最该实测:score_threshold(弃答)、chunk_size/overlap、top_k/fetch_k。
- [2026-07-12] [DEV] RAG-Fusion 纳入 M2:复用混合检索的 RRF 算子(跨查询融合),`rag_fusion_enabled` 默认关。设计为**用户可选模式**:UI(M6)加【深度思考】(开Fusion)/【联网搜索】(强制web)两开关,注入图 State 的 `deep_thinking`/`web_mode` flag,**优先级 用户flag > 自动router > config默认**。已改 config/.env.example/plan(M2/M5/M6)。
- [2026-07-12] [DEV] 多模态方案从**派系1(纯caption)升级到派系2(Multi-Vector)**:检索靠caption文本(复用文本栈不引CLIP),**生成侧命中图片块喂原图给VLM看**(MultiVectorRetriever 绑 caption↔原图)。需生成模型多模态→`multimodal_generation` 开关,M1实测GLM-5.2端点,不支持回退qwen-vl-max/纯caption。加跨页布局锚定。派系3(CLIP)仍跳过。加上下文token预算 max_context_tokens=8000。已改 config/.env.example/plan(M1/M5)。
- [2026-07-12] [DEV] 组织:agent 独立于博客——博客 .gitignore 加 `/rag-server/ /docs/ /*.pdf`,博客 git 零污染(用户选方案B就地隔离,未搬目录)。
- [2026-07-12] [DEV] PDF 解析定**双轨**:主干 A=版面解析器出Markdown→复用md loader;后勤 B=fitz。
- [2026-07-12] [RESEARCHER] R3(报告 docs/collab/research/R3-PDF解析器选型.md,PyPI wheel 实测):**MinerU 在 Python3.14 直接拒装**(requires-python<3.14),Marker/Unstructured/PaddleOCR 亦全 3.14 装不动。**主干 A 选 docling(IBM)**——唯一 3.14 可装,表格TEDS≈91%,自带抽图(PIL+page_no+bbox)→ fitz 缩水为"页数门禁+兜底";docling重~2-3GB放独立[pdf] extra。兜底=LlamaParse(托管,数据出域须拍板)。已改 plan/pyproject。
- [2026-07-12] [DEV] **M1 进行中**:`loaders.py` 的 `load_markdown()` 跑通——205篇→**2790 chunk**,metadata/确定性chunk_id/面包屑全对;切块逻辑抽成可复用 `split_markdown_to_docs()`(PDF复用)。观察:标题分块产生若干"标题空壳小块"→待加最小块合并(M3评测后调)。下一步 ingest.py(需[rag]依赖)。
- [2026-07-12] [DEV] 知识库来源分散→建**来源登记表** `sources.toml` + `sources.py::load_sources()`(stdlib tomllib 零依赖):语料物理分散、逻辑统一登记,ingest 读表按 type 分发 loader,增删改一行不搬文件。实测解析路径 OK(blog_posts 启用/ai_infra_sre 待docling)。Marker(同MinerU)3.14 装不上(Pillow<11 无cp314),docling 保持首选。
- [2026-07-12] [DEV] 统一语料目录:建 `data/sources/{pdf,md}/`(自有语料按类型分),PDF 移入;活语料(博客md)原地引用不拷贝;sources.toml 更新+config 加 sources_dir,验证通过。
- [2026-07-12] [DEV] Python 版本:**[rag] 在 3.14 实测全装成+全导入**(chromadb1.5.9/langchain1.3.13/langgraph/dashscope/rank-bm25);机器无 3.12(仅3.11/3.13/3.14)。结论:换3.12=纯成本零收益(栈已在3.14验证可跑,docling亦3.14可跑,唯MinerU/Marker挡但已弃)。**建议留 3.14**,待用户拍板。
- [2026-07-12] [DEV] 用户定 **3.13**;py 启动器建 venv 报"Unable to create process"→直接用 python.exe 全路径建成(3.13.7)。`[ingest,rag]` 装成、全导入 OK。
- [2026-07-12] [DEV] **key 打通百炼 embedding 端点**(text-embedding-v4 返 1024 维)——一个 DASHSCOPE key 同时通 GLM MaaS + 百炼标准端点。
- [2026-07-12] [DEV] langchain 1.x 漂移:`SQLRecordManager/index` 迁到 `langchain_classic.indexes`(非 `langchain.indexes`)。ingest.py 已改。
- [2026-07-12] [DEV] **M1 md 主线闭环**:ingest 205篇→**2790 chunk 全入 Chroma**(num_added 2790);**重跑 num_skipped 2790、向量仍 2790**(幂等验证通过)。用 DashScopeEmbeddings(社区包将sunset,后迁独立包)+ Indexing API(SHA-1哈希,非对抗场景可接受)。
- [2026-07-12] [DEV] 用户新洞察:**PDF 同时是博客文章**→ docling PDF→MD 一鱼两吃(拆成 posts 发布 + RAG 当普通 md 吃),PDF 塌缩进 blog_md,架构简化。待用户定:分类(AI_LLM vs 新开AI_Infra)+ 拆分粒度;流程=先样张后批量、发布前审。
- [2026-07-12] [DEV] 用户定:**新分类 AI_Infra**、按章节、公开文章严格审。docling 装成(3.13,docling 2.112+torch)。
- [2026-07-12] [DEV] docling 排障:整段(20页)转会 `std::bad_alloc`(内存累积,前~9页成后面崩;关图片生成也不解决)。**单独转 15-19 页=SUCCESS**(4971字干净md,术语无损)→ 确诊内存累积。**修法=分~5页小窗口批处理**(fitz抽小PDF→docling→拼接),图仍走fitz(双轨)。质量达标,拟建批量转换器。
- [2026-07-12] [DEV] **PDF 全本转换成功**:批量转换器(BATCH=5+gc)跑完 **67/67 批 0 失败**,产出 `data/sources/pdf/AI_INFRA_SRE.md`(541KB/26万字)。观察:docling **标题塌平**(4 个 `#`、894 个 `##`、0 `###`)→ 不能按标题级机械拆博客文章,须按文档目录(阶段一~五+命名主题)定章节边界(编辑判断,待用户定粒度)。RAG 入库与博客拆分**解耦**:md 可先入库补 infra 语料(NCCL 缺口),拆文章走"先样张后批量"。
- [2026-07-12] [DEV] 用户定调:**Claude Code = 标杆**,记忆/网关/工具调用/上下文压缩/Agentic 迭代循环照学→翻译成 LangGraph。派 claude-code-guide(机制对照)+ researcher(R4:网关与记忆,已核 LangGraph 1.x API 漂移)双路取证。
- [2026-07-12] [RESEARCHER] R4(报告 docs/collab/research/R4-网关与记忆.md):**网关分两层**——基础设施网关(LiteLLM/Portkey,应用外,运维信号)vs 认知/语义路由(semantic-router/RouteLLM,应用内,按 query 选路)。**LangGraph 1.x API 已核漂移**:短时 `InMemorySaver`(非旧`MemorySaver`)→生产 `SqliteSaver`(独立包 `langgraph-checkpoint-sqlite`);长时 Store API(`put/search`,节点内 `runtime.store` 注入,非旧`get_store()`);State reducer 做黑板。**单用户右尺寸**:不起独立网关服务/不上 Redis,回退用 `.with_retry()/.with_fallbacks()`,语义缓存默认关(旧答案风险)。
- [2026-07-13] [DEV] **GLM-5.2 关思考开关**(实测):`extra_body={"enable_thinking": False}`(或 `thinking={"type":"disabled"}`)→ 分类/判断类调用从 5.5s(268字思考)降到 ~1s、结果不变。**M5 路由/打分器(route_question/grade_documents)应默认关思考**(temp=0 + 无思考 = 更快更确定,不因"想太多"漂移);对话生成保留思考。教训:非流式大输入 + 推理模型会长时间空想→表现为"卡死",且 MaaS 流式偶发 WinError 10054 断连→凡 LLM 调用需重试+韧性。
- [2026-07-13] [DEV] **PDF→博客拆分:清单先行**。用户定粒度=**一主题一篇**。写 `scripts/build_article_manifest.py`(分批+流式+关思考+重试),GLM-5.2 从 894 个塌平 `##` 里判定"文章级主题起点"、清洗标题(修 OCR 数字乱码 BFg549g54F→BF16 等)→ 产出 `data/sources/pdf/article_manifest.{md,json}`,**共 142 篇草稿**。质量:核心主题干净,但有前置摘要残页(封面/学习计划/阶段总览)、过切(PPO×5/DSCP×3/TSDF×2/深度一二三)、约8个标题残留全角OCR字(⽣⽹⼒)→ 需人工/自动策展一遍再拆正文。发布前审。
- [2026-07-13] [DEV] **重大转向:PDF→改用 .lakebook(语雀原始导出)**。用户提供 `AI INFRA SRE.lakebook`=tar 包(`$meta.json` 的 tocYml 目录树 + 151 篇 doc JSON,body 为干净 Lake HTML)。对比 PDF+docling:**零 OCR 损坏、天然 151 篇边界、完整层级、图片是 cdn.nlark.com 真实 URL(与现有博客一致,无需抠图)**。→ 写 `scripts/lakebook_to_articles.py`(tarfile 读 + tocYml 定篇 + markdownify 转 MD + frontmatter),质量开箱发布级(标题层级/列表/代码/公式全对)。生成 **108 篇**入 `src/posts/AI_Infra/`(过滤 43 篇完全空的占位/父节点;规则=正文去空白为0才丢,有图/一句话都留)。装了 markdownify+pyyaml。
- [2026-07-13] [DEV] 用户纪律:**PDF/docling 能力不作废**——这是吃海量异构类型的知识库,每个解析器=按 type 注册的可复用 loader(md/pdf/lakebook/未来 docx/html/csv…),经 sources.toml 分发。**去冗余三层**:①同源幂等(Indexing API+RecordManager 已有)②跨格式同内容→源治理(一份逻辑内容只登记一个规范源;AI INFRA SRE 认 lakebook,PDF 源禁用)③语义近重复→content_hash 去重(M1 loaders 落地)/embedding 相似度剔除(评测后再上)。**AI_Infra 文章落在 posts_dir 下→被 blog_md loader 自动纳入 RAG,一份来源不重复**。待写入 KB 规范。
- [2026-07-13] [DEV] **统一转换管道落地**:`src/blog_rag/converters.py`(按扩展名分派 handler:lakebook/pdf/md/图)+ `scripts/convert.py`(编排:扫 inbox→分派→幂等 sha256→自动 ingest,支持 --force/--no-ingest)。投放约定 `data/inbox/<分类>/`。清掉旧散脚本(lakebook_to_articles/build_article_manifest/manifest_to_articles/probe_thinking)。
- [2026-07-13] [DEV/子agent] **前端 P1+P2 焕新**(用户选 b + 小红书浅色风,派 general-purpose 子agent 做 P2)。P1(dev):theme.ts 身份 王梓涵→Kinger、删 example.com 假社交/encrypt 演示、页脚、hostname 标 TODO;首页 README heroText/标语/6 张项目卡改指向自有分区。P2(子agent,只动 styles/*.scss,取证 theme-hope 真实变量):主色 #f5455c、卡片 15px 圆角+柔和阴影+hover 上浮、浅白底#f7f8fa/纯白卡、亮暗双主题、PingFang SC 行高1.75。真实类 .vp-article-item/.vp-card,accent 走 --vp-c-accent。build 通过(353页无 Sass 报错)。遗留:社交/hostname 待用户填真值。
- [2026-07-13] [DEV] **M2 检索质量落地**(派 researcher R5 取证 API):`retriever.py`(dense / BM25+jieba / **自实现 rrf_fuse 纯函数**共享给 RAG-Fusion / pointwise LLM 重排 GLM-5.2 关思考)+ `rag_chain.py`(单跳,去重 by doc_id + Source-ID 引用 + 弃答)。装 jieba,import 确认 `langchain_community.retrievers.BM25Retriever` / `langchain_classic.retrievers.EnsembleRetriever`。**验证**:NCCL/DCGM 三档对比通过、单跳答案每句带 [S#]+来源且不臆造、库外"红烧肉"正确弃答。**用户定调:不能只用稠密向量**(专有 token 稠密会静默出错,BM25 是兜底保证,hybrid 为默认路径)。
- [2026-07-13] [AUDITOR] M2+管道审计(提交闸门,dev 代记)。**阻塞**:[高1] 接地/弃答硬闸门未接——retrieve 恒返回 top_n,`if not docs` 永不触发,score_threshold/max_context_tokens 代码零引用却在 README/docstring 承诺了 → 库外问题硬喂无关块诱发臆造;[高2] 就地改共享 BM25 单例 Document.metadata(rrf/rerank 写 score)→ 并发跨请求污染+竞态。**中**:[中1] answer(stream=False) 无条件迭代→TypeError;[中2] convert 先写 state 再 ingest→ingest 失败静默漏档;[中3] rrf_fuse weights/doc_lists 长度不等 zip 静默截断;[中4] slug 碰撞覆盖;[中5] lakebook tar 成员缺失整体崩无逐条兜底。**低**:重排无并发/超时/异常、handle_pdf NFKC 损代码块、slugify 未防 `..`、convert 不清孤儿。修复计划:高1 并入双模式(rerank_score 闸门→不达标走通用回答/弃答)+ max_context_tokens 预算;高2 改分数前拷贝 Document;中1/中2 顺手修;中3/4/5 加固。修完 tester 复测 + auditor 复审再提交。
- [2026-07-13] [DEV] **M2 修复落地**(修 auditor 阻塞/中危 + 加用户要的 query 改写/RAG-Fusion):retriever `_with_meta` 拷贝防污染(高2)/rrf_fuse weights 校验(中3)/重排容错/**RAG-Fusion `rag_fusion_search`+`generate_query_variants`**+retrieve(fusion=);rag_chain **双模式接地闸门**(best rerank≥grounding_min_rerank→接地带引用,否则→通用回答标"非KB/无出处"+你可能想问,高1)/`_consume` 修 stream=False(中1)/`_build_context` 按 max_context_tokens 预算;convert 入库成功后落 state(中2);converters `_dedupe_name` 防碰撞(中4)+lakebook 逐 doc try/except+$meta guard(中5);config 加 grounding_min_rerank=5。**验证**:58 测试全绿 + 双模式 live 冒烟(NCCL 接地带引用 / 红烧肉 best=0 走通用知识回答不假引用)。
- [2026-07-13] [TESTER] M2 修复复测:**90 passed/0 failed**(58 基线+32 新增,~10s 零网络)。新覆盖:rrf_fuse 返副本+原对象不变+weights 双向 ValueError、_with_meta、_dedupe_name(不看磁盘)、_build_context 字符预算截断、**双模式闸门离线 mock 测**(grounded/general/无docs/边界值)。未覆盖仍为网络链(dense/hybrid/rerank/ingest 真调用)。
- [2026-07-13] [DEV] **M2 过闸门**:DoD 全绿(tester 90/0 + auditor 可提交 + 双模式 live 冒烟)。M2 交付:retriever(hybrid+RRF+重排+RAG-Fusion+查询变体)/rag_chain(双模式接地闸门+引用+预算)/cli。后续非阻塞:重排器故障降级、你可能想问过滤、Chroma score 归一化(留 M3/M5)。
- [2026-07-13] [AUDITOR] M2 修复复审:**阻塞(高1/高2)+5 中危全解、无回归、可提交**。新中危(后续):**重排器整体故障静默退化为通用**——异常吞成 0 分→端点挂时 best=0→KB 能答的也走通用,且丢弃有效 RRF 序。建议 `_rerank_score_one` 区分"0 分"与"异常",异常比例高→��级到 RRF 序接地+告警。另 dev 记:红烧肉这类彻底库外,"你可能想问"会列无关文档→应仅在 rerank_score>0 才推荐。
- [2026-07-13] [DEV] **M3 评测落地(自实现,弃 RAGAS)**:RAGAS 0.4 硬 import community 已删的 vertexai 路径→与本栈 langchain 1.x 不兼容,改**自实现 metrics.py**(GLM 判官:faithfulness/answer_relevancy/context_recall + 纯算 hit@k/mrr)+ golden.jsonl(10 题,四档 grounded/web/general/refuse)+ run_eval.py。给 rag_chain.answer 追加返回 retrieved_doc_ids/contexts 供评测复用(免重复检索)。**首版基线(n=10)**:hit@k 1.0 / mrr 0.857 / faithfulness 0.9 / answer_relevancy 0.98 / context_recall 1.0 / mode 8/10。7 接地题全优;缺口被量化:GB200 web 题 faithfulness=0.0(无联网→臆造瓦数,justify M4)、手册作者 refuse 题走了 grounded(缺 refuse 分流,M5)。洞察:PagedAttention 命中旧 AI_LLM/LangChain/KV-Cache 排第1、新 AI_Infra 排第2→**跨来源内容冗余**(去冗余议题在评测中显形)。CSV: data/eval_results.csv。pyproject [eval] 清空(自实现无需 ragas)。
- [2026-07-13] [DEV] **M4 联网 + 重排提速**(eval-driven,派 R7 取证)。新 `web.py`(ddgs/tavily/bocha 三 provider 工厂+重试降级返[])+ rag_chain web 兜底分支(不接地→联网带 [W#]+URL,搜不到才通用)+ config(web_search_enabled/tavily_api_key/bocha_api_key/rerank_candidates=10)。**重排 pointwise→listwise**(1 次调用给 top-N 打分,失败退化 RRF 序+及格分,修上轮 auditor 标注的"重排挂→全判不接地"隐患)。**eval 三版对比(n=10)**:faithfulness 0.9→+联网 0.992(GB200 臆造瓦数→引 NVIDIA 文档)→+listwise **1.0**;单次 retrieve ~30s→**~8s**(~4x);hit@k/recall 全程 1.0。文档 docs/collab/性能与调用优化.md。tester 复测 **109/0**(改 3 受影响用例+补 20 web/listwise 用例)。auditor 复闸中。
- [2026-07-13] [AUDITOR] M4 复闸:**红线全过、无高危、可提交**。已解:重排挂→全判不接地(退化 RRF+及格分)、来源不互污(web 仅 [W#]/grounded 仅 [S#] 系统提示隔离)、三 provider 字段映射核实。中/低(dev 处理):[中2] bocha 无 key 白等 4.5s→已加 early-return;[低3] bocha data:null→已链式防 None;[中1] rerank_candidates=10 丢 RRF 11–20(eval hit@k 仍 1.0,可接受,后续对比 10vs20);[低4] **sources schema 随 mode 变**(grounded={sid,doc_id,section}/web={wid,title,url}/general=[])→**未来 API/前端须按 mode 分支解析(契约)**;[低6] config 旋钮无范围校验(backlog)。
- [2026-07-13] [DEV] **M4 过闸门**:eval(faith 1.0/检索~8s/hit@k 1.0)+ tester 109/0 + auditor 可提交。交付:web.py(三 provider 联网,CRAG 兜底)+ rag_chain web 分支([W#]+URL)+ listwise 重排(~4x)。阶梯"本地→联网"通;剩 refuse+router 精分(M5)。M4 完成。
- [2026-07-13] [DEV] 用户定调:**弃答不是唯一兜底**。三种结局:①接地回答(带引用)②**通用回答**(超 KB 范围→LLM 通用能力答,明确标"非知识库/无出处"+"你可能想问…"推荐,绝不套假引用)③弃答(仅本该有据却查不到)。即 plan 的双模式:M5 route_question 分流,通用问题(红烧肉)走通用模式而非死板拒答。已改 plan 设计基调兜底阶梯。rag_chain 加轻量路由版待 tester/auditor 回来后并入(此刻 auditor 正审 rag_chain,不动它)。
- [2026-07-13] [DEV] **发现待修坑**:Chroma `similarity_search_with_relevance_scores` 返回负分/超 [0,1](embedding 未单位归一化 + 默认距离度量)→ 计划的 `score_threshold` 弃答硬闸门失准。修法待定:collection 建 `hnsw:space=cosine`+归一化 或 自定义 relevance_score_fn。当前靠 LLM 自觉弃答(软兜底)可用,硬闸门 M5 护栏前修。另:pointwise 重排 20 次调用偏慢(~30s),M3 后改 listwise/只排 top-N。
- [2026-07-13] [DEV] **pipeline 加第④段"重建前端"**:convert.py 有变更时 转文章→入RAG→`npm run docs:build`(--no-build 关)。端到端验证:投放 lakebook→118篇→**RAG num_added 894 chunk**→docs:build success 25s。至此"投放 data/inbox→转换→入库→前端"一条命令全自动闭环。
- [2026-07-13] [DEV] **存储统一(两区各一根)**:区A=RAG工作区 `rag-server/data/`(inbox/ + chroma_db/ + manifest/record_manager/convert_state + _archive/,全 gitignore);区B=博客发布区 `src/posts/<分类>/` + `public/assets/`(git 跟踪,VuePress 强制不可移)。废弃旧 `data/sources/<type>/`。config 加 inbox_dir/archive_dir/convert_state_path,弃 sources_dir。清理测试垃圾;lakebook 唯一存 inbox(桌面散落副本 hash 校验后删)。已更新 KB 规范 §1 + sources.toml。
- [2026-07-13] [DEV] **AI_Infra 结构重写**:lakebook 的 tocYml 层级 → 嵌套目录(容器→README.md,叶子→.md,frontmatter order 保序)。**重写策略**:透明化冗余壳 `学习计划/阶段一`(STRIP_TITLES)+ 目录最大深度 3(MAX_DIRS,更深拍平)→ 清爽 **5 大分区**(训练架构/大模型技术/深度学习基础/AI-infra/运动学与机械基础),118 篇/36 目录/最深 3 层。navbar 指 5 分区,sidebar children:"structure" 自动渲染全树。空叶子过滤、空容器留 README 当分区索引。
- [2026-07-12] [DEV] **架构定稿并入 plan**:新增「参考 Claude Code 的 Agent 架构」章节(四板块+网关+记忆+ReAct 迭代循环对照表);M8 记忆**提级为一等公民**(checkpointer+Store+State 三层);M5 标注照 Claude(独立子图/前后 Hook/迭代循环);模块树加 `memory.py`。右尺寸原则:学 Hook 拦截模式但不建 Firecracker/沙箱(工具只读+代码不执行→权限面天然小)。
- [2026-07-13] [TESTER] M2 纯逻辑补测落地(rag-server/tests/,只写 tests 未碰源码):5 文件 58 用例全绿(pytest 9.1.1,venv 3.13.7,~10s)。覆盖 rrf_fuse(公式/去重累加/降序/weights/rrf_score round6/id 回退/自定义 id_key/空输入)、tokenize(NCCL/BF16 整词保留、滤空白、中文切开)、_dedupe_by_doc + _build_context(Source-ID 保序/section 回退 title/doc_id 回退 source→"?")、slugify(非法字符/空白转-/截断80/空→untitled/NFKC)、handle_lakebook(构造 tar 验 STRIP_TITLES 透明壳、MAX_DIRS=3 拍平、容器→README/叶子→md/空叶子跳过/空容器留 README/order 保序)、_write 空正文过滤。装了 pytest(此前缺,[dev] extra)。**交 dev 待议(未自行改源码)**:①_dedupe_by_doc 遇 doc_id 与 source 都缺的 doc,key=None 会把多个无关 doc 并成 1 个(现语料 chunk 均有 doc_id,低危);②rrf_fuse 就地改传入 Document 的 metadata(rrf_score 写回原对象,有副作用)。**未覆盖(依赖网络/LLM/Chroma,本轮不测)**:dense/sparse/hybrid_search、llm_rerank、retrieve、rag_chain.answer、ingest 全链、handle_pdf(docling)、真实 lakebook cdn 图。
- [2026-07-13] [TESTER] M2 审计修复复测(只写 tests/,未碰源码):新增 32 用例,全量 **90 passed**(58 基线保持绿,~10s,venv 3.13.7)。覆盖审计四改动点:①rrf_fuse 返回副本——原始 doc.metadata 无 rrf_score(单/多路去重均验)、weights 与 doc_lists 长度不等(过少/过多)raise ValueError、长度相等不抛(回归);②_with_meta 返回副本、原对象 metadata 不变、metadata dict 非共享引用、page_content 保留;③converters._dedupe_name 同 seen 内碰撞追加 -2/-3、保留 parent/suffix、跳过已占后缀、独立 seen 互不干扰、不看磁盘(tmp_path 落真文件仍返原名)、异名不加后缀;④_build_context 字符预算(monkeypatch max_context_tokens 调小)超预算砍靠后块、至少留1、恰好=budget 不砍、全在预算内全留。⑤answer 双模式闸门——**未跳过,用 monkeypatch 替身 retrieve+get_client 离线测**:best≥grounding_min_rerank→grounded(带 sources)、below→general(sources=[]、suggestions 推荐)、无 docs→general、边界值(=阈值 grounded / 阈值-1 general)、best 取 docs 内 max。全离线零网络/LLM/Chroma。**未覆盖(依赖网络/外部,本轮不测)**:dense/sparse/hybrid_search、llm_rerank 真实调用、retrieve 全链、ingest、handle_pdf(docling)、真实 lakebook cdn 图。
- [2026-07-13] [TESTER] 两处行为变更测试更新+补齐,全量 **109 passed / 0 failed**(~10s,离线零网络,venv 3.13.7)。**变更1(rag_chain WEB 兜底)**:改 3 个原失败用例(general_when_best_below_threshold/general_when_no_docs/just_below_threshold_is_general)——stub `rag_chain.web_search` 返 [] 测 general 路径;新增 4 例:web_search 返样例→mode='web'(sources 为 {wid,title,url} URL 出处、不掺 [S#]、contexts 取 snippet)、无 docs 也先联网、web_search_enabled=False 跳联网直 general(_boom 守卫证 web_search 未被调)、接地路径不联网。**变更2(retriever.llm_rerank listwise)**:新建 test_retriever_llm_rerank.py(16 例,stub `retriever.get_client`)——正常解析按分降序+写 rerank_score、正则吃 [0]=/1：/2: (方括号+全角/半角冒号)、��� clamp≤10、越界编号忽略、top_n 截断、只排 candidates 切片;退化(乱输出/空串/None content/抛异常)→ RRF 序+每篇 grounding_min_rerank、退化也守 top_n;空候选→[]、不改原对象(_with_meta 拷贝,正常+退化双验)、显式 top_n 覆盖 setting。`_rerank_score_one` 全库无引用(已随源码删)。**未覆盖(依赖网络/外部)**:web.web_search 真实 provider(ddg/tavily/bocha)、build_web_context 仅随 web 路径间接覆盖、retrieve/hybrid/dense/sparse 全链真调用、ingest、docling。遗留提醒:源码 retriever.py:14 langchain-community DeprecationWarning(既存,非本次)。
- [2026-07-14] [TESTER] M5 CRAG+路由+A/B开关 离线补测:全量 **188 passed / 0 failed**(109 基线保持绿 + 79 新增,~11s,零网络/LLM/Chroma,venv 3.13.7)。只写 tests/,未碰源码。3 新文件:test_graders_route.py(_prefilter 各类通用词命中/技术问题NCCL/BF16/CUDA/TCP→None/大小写/子串;route_question 三层守卫——web_mode→rag 且 _boom 证不调 classify、预筛命中→general 不调 classify、都不中→调 classify stub、web_mode>预筛优先级;classify_route stub get_client 返 general/rag/未知→rag/None→rag/异常→rag);test_graders_gates.py(grade_documents 阈值过滤+恰好=阈值边界保留+缺 rerank_score 当0+空/缺键→[]+不调LLM守卫;grounding_gate 新语义 graded非空→grounded[含假弃答回归守卫+忽略best_score]/空+有额度→rewrite/空+额度尽+web开→web/web关→refuse/web_mode短路→web/loop_step==max边界/回落settings.max_retries;grade_web 空→False/yes→True/no→False/异常→True保守放行/缺字段不抛;after_web 无结果→refuse/不相关→refuse/相关→web_generate/缺字段默认True);test_graph_and_config.py(build_graph 编译+11业务节点集合精确匹配+含新增grade_documents/grade_web+get_graph单例;config RAG_USE_GRAPH 别名——字段 validation_alias 断言+setenv=1→True/=0→False/无env→False/裸 USE_GRAPH 不再映射[回归 bug 本身])。**未发现源码疑点**,源码行为与设计一致。未覆盖(依赖网络/LLM/Chroma):retrieve/web_search 真实调用、图端到端 invoke。
- [2026-07-14] [DEV] **M5 收口裁定(用户反馈驱动的结构化重构)**。用户批评:决策没结构化、别打关键词补丁、加冗余、参考 Claude、我是来学的。两条线落地:**线A 文档结构化**——docwriter 建 `docs/collab/{architecture.md,roadmap.md,decisions/}`,ADR 模板+回填 12 条历史选型(0001–0012)+ dev 写 3 条新决策(0013 路由形态/0014 弃答机制CRAG/0015 checkpointer);**线B 原则性多道防线替关键词 hack**——路由加**确定性预筛+LLM**(三层)、弃答改 **CRAG 证据打分**(`grade_web` 生成前拦垃圾联网、`grade_documents` 细筛,关键词归一降为兜底且**只看开头**防误伤正文否定词)。**抓到真 bug**:`use_graph` 未加别名→`RAG_USE_GRAPH` 静默失效→此前评测全跑 M2 基线没走图(已加 validation_alias)。**auditor 抓到真回归**:grade_documents 拿阈值砍生成上下文→丢 rerank 1–4 分证据→假弃答;已改 grounded_generate 喂全量 documents(grade 只当接地信号)。**闸门**:tester 188/0、auditor 可提交(无阻塞)。**eval(真走图,n=10)**:hit@k 1.0/faithfulness 0.967/context_recall 1.0/**mode 9/10**——红烧肉 general✅、手册作者 refuse✅、agentic-rag grounded✅(护栏开头判修复);浮动的 1 个是 GB200(免费 DDG 被我多轮跑限流→返垃圾→grade_web 正确判无关→诚实 refuse,符合"绝不臆造";属 provider 质量,非逻辑 bug,配 Tavily/博查 key 可稳)。**不在 n=10 过拟合调阈值**。M5 完成。
- [2026-07-14] [DEV] **M6.1 核心对话闭环完成**(gr.Blocks,分两闸的第一闸)。交付:①**流式地基**——`llm._emit_token` 经 `get_stream_writer()`(langgraph.config)发 custom token 事件(三重 guard,CLI/eval 零回归),`graph.stream_answer` 生成器 `stream_mode=["custom","values"]` yield token+末尾 done(契约同 answer_graph);②`feedback.py`(👍👎→JSONL,字段对齐 golden 零转换回流)+`load_feedback`;③`app.py`(左对话流/右来源面板按 mode 渲染/顶深度思考·联网开关→图 flag/👍👎→落盘/thread_id 接 checkpointer);④ADR-0016 流式选型(custom writer vs messages:节点走裸 openai SDK,messages 拿不到 token);⑤pyproject `ui` extra(gradio 6.20)。**选型**:UI=gr.Blocks(全控制)、分两闸(先核心后增强)——用户定。**流式实测**:NCCL 19 token 逐个 yield;chat_fn headless 14 帧、来源"📚接地"、快照带 doc_id。**闸门**:tester 225→**232 passed**(含 verdict 回归 7);**auditor 抓到高危并修复**——`feedback_options` 用自定义中文标签时 gradio 6.20 回传的是**标签字符串非 bool**,`if data.liked` 恒真→👎 被记成 👍(负反馈全反转,tester mock bool 假绿没抓到);查 gradio 源码确认,改用精确 `("Like","Dislike")` 拿回 bool+拇指图标,并加 `_verdict_of` 对 bool/字符串双鲁棒 + 回归测试。顺带修两低危:空输入覆写 last_exchange(传入保留)、like_fn 加 try/except。**教训**:mock 的类型要贴合真实前端契约,否则假绿。M6.2 待做:Mermaid 自动修复渲染/代码高亮/重生成/简短·详细/👎纠正输入框(补 correction→golden reference 闭环,tester 标注的接口点)。M6.1 完成。
- [2026-07-19] [DEV] **agent 部署(M7 第一闸,产物已实现)**。用户要"博客和 agent 都上线"+有服务器 SERVER_IP(无域名)。厘清关键区别:**博客=静态站(GitHub Actions→Pages,早已自动上线)**,agent=**活的 Python 服务**(每问要现算检索+GLM+图流转)→ 静态托管跑不了,必须常驻后端。探测:端口 22 开、80/443/7860 空闲(干净机)、torch 527M 属 docling 入库期依赖(运行时全 API 调用不需要)、chroma 库仅 33M。选型见 **ADR-0017**:自建 + Docker/compose + tar-over-ssh + Gradio auth + 先 IP 后 HTTPS。**实现**:config 加 `ui_host(默认127.0.0.1安全)/ui_port/ui_auth_user/ui_auth_pass`;app.py launch 读 config + `show_api=False`;`Dockerfile`(slim+清华源+只装 rag/agent/web/ui+`pip show torch` 验证无 torch);`docker-compose.yml`(挂 .env/data 卷、restart:unless-stopped、healthcheck、硬覆盖 UI_HOST=0.0.0.0);`.dockerignore`(.env/data 不进镜像层);`deploy.sh`(预检 docker + tar-over-ssh 推送 + 远程 compose up)。文档 `docs/collab/部署.md`。**待办**:①上服务器执行(需 SSH 密码)②第二闸 nip.io+Caddy HTTPS(IP 阶段明文是已知弱点)③国内注意:DDG 联网被墙,要联网需换 bocha/tavily key。**未过闸**:tester/auditor 待跑。
- [2026-07-14] [DEV] **代码能力(生成+高亮,不执行)**。用户问"怎么让他有代码能力"。实测发现:编程请求("写快排/写装饰器")被**误路由到 rag**,进了接地契约(只用资料、禁用资料外知识)→ 模型即使会写也**拒答**(mode=refuse)。**根因=双模式设计的必然**:让 RAG 精准的"只用资料"铁律正好扼杀代码生成 → 代码必须走 general(自由用自身知识)。修:①`graders._ROUTE_SYS` 加 few-shot 区分**生成型→general**(写代码/实现算法/正则/SQL/Mermaid)vs **要事实→rag**("博客里 X 怎么讲的");②`general.py` 用代码感知 `_GENERAL_SYS_M5`(```语言 围栏→Gradio 高亮、可运行、**只生成不执行**),M2 `rag_chain._GENERAL_SYS` 冻结不动。**live 验证**:快排/计时装饰器/登录 Mermaid 三类全 mode=general + 围栏输出。**eval 无回归**:7 事实题仍全 rag/grounded、红烧肉 general、faithfulness 0.98/hit@k 1.0/mode 9/10(GB200 仍 DDG 限流噪声)。选型=生成+高亮(用户定),不接 E2B 执行(守"代码不跑→权限面天然小")。232 测试绿、无悬空引用。归入 M6。
- [2026-07-23] [TESTER] astro-site 阶段1 终验(只读+跑 build,未碰源码)。**build exit 0**。**抓到 1 个必修问题(非源码,构建缓存)**:demo 文章已从 src/content/posts/ 删除,但 `node_modules/.astro/data-store.json`(gitignore 的持久内容缓存)残留旧 demo 条目→getCollection("posts") 仍返 1 篇→blog 列表渲染"共 1 篇"+"模板验证"卡片(**非空态**)且生成幽灵页 dist/blog/_demo/index.html。清 `.astro` 无效,必须连 `node_modules/.astro` 一起清。**已清缓存重建验证**:源码本身正确——干净构建=2 页(index+blog/index)、blog 显示"共 0 篇"+"文章即将上线"空态、无 _demo。风险:本机 scp 部署 dist 前若不清 node_modules/.astro 缓存会把 demo 带上线(建议 build 前 `rm -rf .astro node_modules/.astro dist`)。已把本机缓存与 dist 清成正确态。**reduced-motion 唯一遗漏**:blog/index.astro:88-92 `.chip{transition:all .15s}` 未进 @media(prefers-reduced-motion) 守卫(141-145 只列 .card/.bar/.arrow);仅色彩过渡无位移、且空态下 chip 不渲染(潜在),建议补 `.chip{transition:none}`。其余 8 组件+文章页守卫全覆盖。**其余全通过**:落地页组件产出齐(Hero/Stats/Features/TechStack/AIEntry)、SERVER_IP 仅在 config.ts(Nav/Hero/AIEntry 用 SITE.aiUrl)、5 图标齐全+BaseLayout ?v=2 引用+logo/favicon 均节点拓扑非 Vue、dist/node_modules/.astro/_shots 均已 gitignore 未跟踪。
- [2026-07-23] [DEV] **博客重构阶段2:Obsidian vault → Astro 内容迁移完成**。事实源从 VuePress src/posts 改为 `D:/obsidian workspace`(用户定,全量迁)。`scripts/migrate-obsidian.mjs`:遍历 545 篇→跳过 16(12 占位桩文件名/4 冲突副本/1 vault README,auditor 核实无误杀)→迁 529 篇(78 空占位桩 type:placeholder/正文"待补充" 标 draft:true 隔离,列表 451 篇)→英文目录名映射中文分类(CATEGORY_MAP,12 类)→**不显示日期**(vault mtime 全是导入日不可靠,用户定;列表按 title.localeCompare 排)→Obsidian 语法转换(wiki[[x]]→纯文本、图片嵌入![[x]]→![](x))→中文 slug 保留(去括号/空格)。**图片本地化**:各 assets/ 拷入 content/posts,IMG_RE 正则容忍括号文件名(MySQL(关系型数据库)-9.png),**三基准定位**(md 同级/父目录/vault 根)——auditor M1 抓到"同名子目录布局 assets 在父级"漏 42 张图,加 dirname(mdDir) 基准后拷贝 435→477、缺失 43→1(那 1 张 vault 里真没有)。**踩坑填坑**:①KaTeX strict 模式→真实笔记 $ 不都是公式→改 throwOnError:false/strict:false(auditor 确认无 XSS,KaTeX trust:false 默认禁 \href/\html);②括号文件名正则截断→修 IMG_RE 用扩展名锚点;③构建缓存幽灵页→加 build:clean(rm .astro+node_modules/.astro+dist);④slug 碰撞静默覆盖隐患→加 seenPaths Set 检测加后缀(L2)+空 slug 兜底 untitled(L3)。**闸门**:tester 7/7 全绿(531 页 build exit 0、451 上线、图片解析抽查、中文 slug 路由、5 篇跨分类渲染非空)、auditor 无阻塞可提交(明确通过 KaTeX 安全/schema strArray/build:clean/无敏感文件泄漏/slug 零碰撞)。**git 建议(auditor)**:content/posts 图片 87MB 必须进 git(博客构建输入,不能 gitignore),远低于 GitHub 100MB 限,将来涨大再上 LFS。astro-site 整体尚未 git add,提交时机用户定。阶段2 完成,待阶段3(备案后 nginx/HTTPS 部署)。

---

## [2026-07-24] [DEV] 博客回归原版 VuePress + 三件大改(换机交接快照)

### 决策转折(重要,避免下台机器重蹈覆辙)
经多轮澄清,**最终定案:放弃 Astro 重写,回到 GitHub 原版 VuePress theme-hope 博客**(功能齐全:搜索/评论/深浅切换/目录/时间线/分类)。
- **astro-site/ 已弃用**(独立目录,不删不用,已 gitignore,不影响原版)。之前误把"改样式"理解成"重写整站",走了大弯路——**下台机器别碰 astro-site,只在原版 VuePress(src/ + src/.vuepress/)上做事**。
- rag-server/ 也 gitignore,是独立的 AI 助手项目(部署在 SERVER_IP:7860,前端已改深色待用户 scp)。

### 已完成的三件大改(都在原版 VuePress 上)
1. **文章同步(全量替换 src/posts)**:事实源=Obsidian vault `D:/obsidian workspace`。脚本 `scripts/sync-obsidian-to-vuepress.mjs`(dry-run 已验)。545 篇扫描→529 迁入(78 空占位桩标 article:false 隔离)→跳过 16。保留原中文文件名(不 slugify,URL 现状一致)。图片本地化到 `src/.vuepress/public/assets/posts/`(344 张去重),引用改 `/blog/assets/posts/...` 绝对路径。修了语雀 `1\.` 转义标题。frontmatter 生成 theme-hope 格式(title/icon 按分类/date git 首提交/category 数组)。10 个中文分类:AI基础设施195/AI大模型108/计算机网络64/面试58/杂项笔记33/运维26/云原生21/开发12/AIOps平台9/数据库3。**双备份**:git 跟踪原 posts + `_backup_posts_orig/`(gitignore),`git checkout src/posts` 可还原。
2. **重整分类**:`sidebar.ts` prefix 改中文目录名(structure 自动跟随)。`navbar.ts` 重写=分类聚合页 `/category/<中文名>/`(blog 插件自动聚合,零死链,不再硬编码文章文件名)。旧 navbar 有死链(服务器资源预警平台挂错 DevOps 等)已消除。
3. **深色科技风**:`theme.ts` darkmode:"switch"(保留亮/暗/自动切换按钮,用户要保留)。`config.ts` head 加内联脚本首次默认深色(localStorage key=vuepress-theme-hope-scheme,值 JSON 序列化"dark";放 head 而非 client.ts,避免"点两次才切换"时序 bug)。`palette.scss` dark 变量调科技风:底#08090a/卡片#16181d/发丝线边框/accent 靛紫#5e6ad2(与落地页/AI助手同色)。`config.scss` $theme-color=#5e6ad2。`index.scss` 浅色硬编码改跟随主题(导航栏毛玻璃深浅两套)。切换禁过渡(.color-switching 类,client.ts MutationObserver 打)防卡顿。

### 交互修复 + Taste 审美收敛(最后一轮)
- 修:顶栏浅色配色/点两次切换/切换卡顿/AI悬浮球图标丑+压回到顶部按钮(悬浮球换机器人 SVG 图标,bottom:88px 让开回到顶部,纯靛紫非渐变)。
- 装了 taste-skill(`.agents/skills/design-taste-frontend`,Safe/Low Risk)。按其 editorial/blog 预设(VARIANCE6/MOTION4/DENSITY3)审计并收敛 3 处 AI 土味:卡片 hover 发光边框→纯净提亮、hero 负字距别挤中文、AI悬浮球渐变→纯色。

### 待办(下台机器接续)
- **未过闸**:tester(docs:build 529篇零错+抽查渲染)+ auditor(脚本健壮/无死链/数据安全)还没跑。
- 用户尚未在浏览器最终验收深色/切换/分类/文章/图片/悬浮球。
- rag-server 前端深色版待用户 scp 上线(scp src/blog_rag/static/index.html root@SERVER_IP:/root/blog-rag/src/blog_rag/static/)。
- 备案通过后:域名 venking.tech + HTTPS + AI 助手改 iframe 内嵌(见 domain-https-plan 记忆)。

### 关键文件(下台机器 clone 后看这些)
- `scripts/sync-obsidian-to-vuepress.mjs`(同步脚本,改 Obsidian 内容后重跑)
- `src/.vuepress/{theme,navbar,sidebar,config,client}.ts` + `styles/{palette,config,index}.scss`
- `src/posts/`(529 篇已迁,中文分类目录)
- `docs/collab/planning/blog-3col-layout.md`(astro 三栏方案,已弃用)

---

## [2026-07-24] [DEV] M7/M8 收尾与追问检索改写

- **M7 验真**:线上容器 `running/healthy`,restart=0;`/ai/health` 200。SDK timeout=60s/max_retries=2 生效;`reliability.py` 主端点可重试错误切备用 provider 的离线测试补齐。生产未填 `FALLBACK_BASE_URL/FALLBACK_MODEL`,所以备用降级代码就绪但未启用。
- **M8.1 短时记忆验真**:`SqliteSaver` 落 `data/checkpoints.sqlite`;新增真实集成测试,第一轮写入后关闭连接、重新打开 saver,同 thread 能读回第一轮并追加第二轮。
- **M8.2 追问改写完成**:新增 `contextualize_query` 图节点。RAG 路径检索前把「它有什么作用」结合最近历史改成独立查询;首轮无历史零额外调用;失败/异常回退原问题。Reflexion 回环与 web fallback 继续使用补全后的 `search_query`,原始 `question` 保持不变供最终回答。
- **测试债修复**:旧 `test_graph_and_config.py` 仍断言 11 节点,漏掉已上线的 `record_turn`;现更新为 13 节点并覆盖 `contextualize_query`/`record_turn`。新增 `test_memory.py`、`test_reliability.py`。
- **闸门**:生产 FastAPI/Agentic 路径 **228 passed / 0 failed**,Ruff 全绿。两个旧 Gradio 备用 UI 测试因轻量生产镜像不装 `[ui]` 而未纳入该次容器运行;当前线上不走 Gradio。
- **仍未完成**:长期 Store/用户画像(需先定义 user_id namespace,防公网用户串记忆);备用 provider 实际配置;域名/HTTPS(等备案);Git 首次提交。

---

## [2026-07-24] [DEV] 换机接手:构建闸门 + auditor 提交前复审 + README 首推

- **接手上下文**:新机(macOS, Node 24)clone 后接续。读完 HANDOFF/roadmap/notes 交接链。
- **tester 闸门(构建)**:清缓存后 `npm run docs:build` **通过**,705 页,退出 0,无真实渲染错误。唯一噪音=`@vuepress/plugin-git` 因仓库 0 commits 对每篇报 `git log exit 128`(531 条),导致文章缺日期/时间线 → 首次提交后自动消除。
- **README 首推**:按用户给的 GitHub 片段,`git init` + 仅 `git add README.md` + commit + `remote add origin https://github.com/ZH-Kinger/venking.git`。跳过片段里会污染 README 的 `echo "# venking"`。**代理坑**:git 全局 `http(s).proxy=127.0.0.1:7890` 但代理没开→push 连不上;直连 github:443 正常,遂本条命令临时 `-c http.proxy= -c https.proxy=` 直连推成功(未改全局配置)。main 已跟踪 origin/main。**注意:仅 README 上线,博客源码/rag-server/astro-site 尚未进 git。**
- **auditor 提交前复审(2070 将跟踪文件,含 rag-server 74/astro-site 1041)**:
  - 密钥扫描 **✅ 干净**,零凭据泄漏;`.env*` 正确 gitignore;`api_key` 全从 env 读。
  - **[中] 生产服务器暴露**:`SERVER_IP`+`root@`+开放端口散落 14 处(navbar.ts:34/client.ts:17/README/rag-server deploy.sh/docs)。公开仓库=公开攻击面,需用户知情接受。
  - **[中] deploy.sh 鉴权误导**:deploy.sh:28-29 预检 `UI_AUTH_USER/PASS`,但 FastAPI 版(api.py)**无任何 auth**,端点全公开→DASHSCOPE key 额度可被白嫖。
  - **[中] navbar :7860 直连 vs README 回环矛盾**:navbar/client 指 `SERVER_IP:7860`,README 称 7860 仅绑 127.0.0.1 只经 nginx `/ai/`。二者只能一真;建议统一走 `/ai/`。
  - **[低] MaaS 实例 ID 硬编码**:config.py:27 `ws-...maas.aliyuncs.com`(非密钥)。
  - **同步脚本健壮性(非提交阻塞,下次重跑前修)**:rmSync(src/posts) 在 walk(VAULT) 之前无条件执行+VAULT 硬编码 Windows 路径→路径错会先删光已发布文章;图片按 basename 去重会串图;CRLF 致 `^---\n` frontmatter 解析失败;gitFirstDate 相对路径插双引号 shell 串有注入风险。
  - **死链 ✅**:10 个中文分类目录 ↔ sidebar 10 prefix ↔ navbar 10 `/category/`↔ 529 篇 category 值,一一对应无孤儿。
  - **裁定**:凭据角度可安全提交;前提是用户接受公开仓库暴露 IP/root/端口/MaaS ID。

---

## [2026-07-24] [DEV] 脱敏 + 完整初始提交推送 GitHub

- **用户决策**:脱敏后完整推送(博客主线 + rag-server + astro-site 全进公开仓库 ZH-Kinger/venking)。
- **脱敏**(仅动将跟踪文件,article 正文里的 `[root@host]#` shell 提示符等技术内容未碰):
  - AI 入口 `navbar.ts` / `client.ts`:`http://SERVER_IP:7860` → 同源相对路径 `/ai/`(经 Nginx 反代,顺带修掉 auditor 报的“navbar 直连 vs README 回环”矛盾 + 消除端口暴露)。
  - 用户可见 README(根 + rag-server):生产 IP → 域名 `venking.tech`(http→https)。
  - 部署脚本 `deploy.sh`:`SERVER` 默认值去掉,改 `${SERVER:?...}` 必填;完成提示改经 Nginx 域名。
  - MaaS 实例 ID(`config.py` / `.env.example`):`ws-...maas` → `YOUR-MAAS-INSTANCE` 占位;真实值靠 `.env`(gitignore)覆盖。
  - 内部 docs(notes/部署.md/ADR-0017):IP → `SERVER_IP` 占位,MaaS 实例 → 占位。
  - 复核:将跟踪文件里 `115.191.2.86` / `ws-iwb5x2xssjmnh9zy` / `root@115` 全部清零。
- **构建复验**:脱敏后 `docs:build` 仍 exit 0,705 页,成功。
- **提交**:因首个 README-only 提交已含 IP,遂 `git commit --amend` 把脱敏后的全部 2070 文件并入唯一初始提交(`d84ed95`),force-push 覆盖,含 IP 的旧提交 `9911e11` 从分支移除(不可达待 GC)。远端 main 已确认 = d84ed95。
- **代理**:全局 git 走 127.0.0.1:7890 但代理没开;本次 push 用 `-c http.proxy= -c https.proxy=` 直连(未改全局)。
- **残留提醒**:①Astro 废弃博客仍随仓库入库(423M,含与 VuePress 重复的文章内容),体积/困惑负担未清;②AI 端点无鉴权、同步脚本破坏性删除仍未修;③`:7860` 是否真只绑回环需上线实查。
