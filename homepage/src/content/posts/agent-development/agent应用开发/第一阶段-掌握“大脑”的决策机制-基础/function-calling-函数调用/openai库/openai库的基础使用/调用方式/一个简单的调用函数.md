---
title: "一个简单的调用函数"
date: 2026-07-23
category: "Agent 开发"
---
```plain
from openai import OpenAI
# 假设你的配置文件叫 settings.py，且在同一目录下
from config.settings import settings
from openai.types.chat import ChatCompletionMessageParam
from typing import List

def basic_chat():
    # 1. 使用 settings 对象初始化客户端
    client = OpenAI(
        api_key=settings.API_KEY,
        base_url=settings.BASE_URL
    )

    # 2. 构造对话
    messages: list[ChatCompletionMessageParam] = [
        {"role": "system", "content": "内容"},
        {"role": "user", "content": "你好"}
    ]

    # 3. 发起请求，全部引用 settings 中的变量
    response = client.chat.completions.create(
        model=settings.MODEL_NAME,      # 使用 settings 里的 qwen-max
        messages=messages,
        temperature=settings.TEMPERATURE, # 使用 settings 里的 0.3
        max_tokens=settings.MAX_TOKENS  # 调用配置里的值
    )

    print("模型回复：", response.choices[0].message.content)

if __name__ == "__main__":
    basic_chat()
```
